listener 1883
protocol mqtt

listener 9001
protocol websockets

# Secure WebSocket (port 9002) — requires valid TLS certs
# Disabled until certs are regenerated (DOMAIN must be set in .env)
# listener 9002
# protocol websockets
# certfile /mosquitto/certs/fullchain.pem
# keyfile /mosquitto/certs/privkey.pem

allow_anonymous true
