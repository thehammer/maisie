{
  "name": "@maisie/callimachus",
  "version": "0.1.0",
  "description": "Queryable index over arbitrary corpora — books, codebases, wikis — exposed to LLMs as a structured tool surface",
  "main": "src/index.ts",
  "bin": {
    "calli": "./src/cli/index.ts"
  },
  "scripts": {
    "test": "bun test",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0",
    "@maisie/shared": "workspace:*",
    "@modelcontextprotocol/sdk": "1.9.0",
    "drizzle-orm": "^0.38.0",
    "hono": "^4.12.18",
    "zod": "^4.3.6",
    "zod-to-json-schema": "^3.25.2"
  },
  "optionalDependencies": {
    "@maisie/callimachus-adapter-book": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
